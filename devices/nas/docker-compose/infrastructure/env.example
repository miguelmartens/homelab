# Tailscale Configuration

# Tailscale Auth Key (get from https://login.tailscale.com/admin/settings/keys)
# Create one at: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxxxxx

# State directory for Tailscale (persists machine key)
TS_STATE_DIR=/data

# Routes to advertise (adjust to match your LAN subnet)
TS_ROUTES=192.168.1.0/24

# Accept DNS configuration from Tailscale (recommended)
TS_ACCEPT_DNS=true

# Optional: Extra Tailscale arguments
# Examples:
# - Advertise as exit node: --advertise-exit-node
# - Hostname for this node: --hostname=my-nas
# TS_EXTRA_ARGS=
