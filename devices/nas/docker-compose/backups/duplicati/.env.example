# Duplicati Configuration

# Timezone
TZ=Europe/Amsterdam

# User and Group IDs (match your NAS user)
PUID=1000
PGID=1000

# Web UI Port
DUPLICATI_PORT=8200

# Web UI Authentication (REQUIRED - set strong passwords!)
DUPLICATI__WEBSERVICE_USERNAME=admin
DUPLICATI__WEBSERVICE_PASSWORD=change_me_to_strong_password

# Optional: Encrypt Duplicati's settings database at rest
SETTINGS_ENCRYPTION_KEY=

# Configuration Directory
CONFIG_DIR=/data

# Backup Target Directory (optional, for local backups)
BACKUP_DIR=/data

# Source directory to backup (update with your actual path)
BACKUP_SOURCE=/data

# Alternative example paths:
# BACKUP_SOURCE=/volume1
# BACKUP_SOURCE=/nas/shared
# Use specific mounts in compose file instead:
# - /volume1/Documents:/source/Documents:ro
